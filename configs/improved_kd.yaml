experiment_name: improved_kd_shuffle
defaults:
    - callbacks: default
    - data: mnist
    - label_smoother: identity
    - misc: default
    - model: lenet
    - lightning_module@orig_module: standard
    - lightning_module@distill_module: improved_kd
    - sampler: standard
    - scorer: standard
    - trainer: default
hydra:
    run:
        dir: results/${data.name}/${model.name}/improved_kd/${distill_module.params.alpha}/${misc.seed}
    output_subdir: .